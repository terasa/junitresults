version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: mkdir
          command: mkdir -p /tmp/phpunit/junit
      - run:
          name: Short
          command: cp short.xml /tmp/phpunit/junit/junit.xml
      - store_test_results:
          path: /tmp/phpunit
          
      
