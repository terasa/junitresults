version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: mkdir
          command: mkdir -p ~/phpunit
      - run:
          name: Short
          command: cp short.xml ~/phpunit/junit.xml
      - store_test_results:
          path: ~/phpunit
          
      
